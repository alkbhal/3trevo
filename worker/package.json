{
  "name": "tres-trevo-api",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.106.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
