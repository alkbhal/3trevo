name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  stamp-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verifica se os hashes de cache-busting batem com o conteudo real
        run: node scripts/stamp.mjs --check

  worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx vitest run
